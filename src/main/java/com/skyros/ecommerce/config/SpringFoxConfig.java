package com.skyros.ecommerce.config;


public class SpringFoxConfig {

    /*
    @Bean
    public Docket api() {
        return new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(RequestHandlerSelectors.any())
                .paths(PathSelectors.any())
                .build()
                .apiInfo(metaData());
    }

    private ApiInfo metaData() {
        Contact contact = new Contact("Ahmed Baz", "https://facebook.com/ahmedbaz1024/", "ahmed.baz@fawry.com");
        Collection<VendorExtension> vendorExtensions = new HashSet<>();
        ApiInfo apiInfo = new ApiInfo(
                "Spring Boot REST API",
                "Spring Boot REST API for Online E-Commerce App",
                "1.0.0",
                "Terms of service",
                contact,
                "Apache License Version 2.0",
                "https://www.apache.org/licenses/LICENSE-2.0",
                vendorExtensions);
        return apiInfo;
    }
     */

}

